defmodule Kvstore.Utils do

  def valid?(:data, data, fun) do
  end

  def valid?(:data, old_data, new_data, fun) do
  end

  def valid?(:keys, keys) do
  end

  def valid?(:values, data) do
  end

end
